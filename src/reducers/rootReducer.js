import { combineReducers } from "redux";

import questionPaperDataReducer from "./questionPaperDataReducer";

const rootReducer = combineReducers({
  questionPaperDataReducer
});

export default rootReducer;
